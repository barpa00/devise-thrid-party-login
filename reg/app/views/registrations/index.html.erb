<h1>root</h1>
<%= flash[:notice] %>

<%# <% if session[:userooo].present? %>
<%# <%= User.find_by(id: session[:userooo]).nickname %>

<ul>
  <% if current_user.present? %>
  <%= current_user.nickname %>
    <li><%= link_to "登出", 'sign_out' %></li>
  <% else %>
    <li><%= link_to "註冊", 'sign_up' %></li>
    <li><%= link_to "登入", 'sign_in' %></li>
  <% end %>
</ul>