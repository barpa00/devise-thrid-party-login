<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to '登出', destroy_user_session_path, method: :delete, class:"nav-link active" %> 
        </li>
        <li class="nav-item">
          <%= link_to '修改密碼', edit_registration_path(:user), class:"nav-link active" %> 
        </li>
        <% else %>
        <li class="nav-item">
          <%= link_to '註冊', new_registration_path(:user), class:"nav-link active" %> 
        </li>
        <li class="nav-item">
          <%= link_to '登入', new_session_path(:user), class:"nav-link active" %> 
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
    